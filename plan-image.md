┌──────────────┐
│   You (人)   │
└──────┬───────┘
       │
       │ 1️⃣ 提需求
       │   - 新建 Issue
       │   - 描述需求
       │   - 打标签: plan:needed
       ▼
┌────────────────────┐
│      GitHub        │
│  Issue / Labels    │
└──────┬─────────────┘
       │
       │ 2️⃣ GitHub Action 监听 plan:needed
       ▼
┌──────────────────────────┐
│ Claude Code GitHub App    │
│ (Plan Model)              │
└──────┬───────────────────┘
       │
       │ 3️⃣ @claude 生成 Plan
       │   - Goal / Steps
       │   - Acceptance Criteria
       │   - Files / Commands
       ▼
┌────────────────────┐
│      GitHub        │
│  Issue Comment     │
│  + plan:approved   │
└──────┬─────────────┘
       │
       │ 4️⃣ GitHub Action 监听 plan:approved
       ▼
┌──────────────────────────┐
│        Codex GitHub App   │
│   (@codex 委派执行)       │
└──────┬───────────────────┘
       │
       │ 5️⃣ Codex Cloud 执行
       │   - clone repo
       │   - 按 Plan 改代码
       │   - 跑 tests / build
       │   - git push
       │   - 创建 PR
       ▼
┌────────────────────┐
│      GitHub        │
│   Pull Request     │
└──────┬─────────────┘
       │
       │ 6️⃣ PR opened / synchronize
       ▼
┌──────────────────────────┐
│ Claude Code GitHub App    │
│ (Review Model)            │
└──────┬───────────────────┘
       │
       │ 7️⃣ @claude Review
       │   - 对照 Acceptance Criteria
       │   - 给出结论
       │
       │   ┌───────────────┐
       │   │ 分支 A：通过   │
       │   └───────────────┘
       │        │
       │        │ 8A️⃣ merge:approved
       │        ▼
       │   ┌────────────────────┐
       │   │ GitHub Action       │
       │   │ 自动 merge PR       │
       │   └────────────────────┘
       │
       │   ┌────────────────┐
       │   │ 分支 B：需修改  │
       │   └────────────────┘
       │        │
       │        │ 8B️⃣ changes:requested
       │        │     needs:codex
       │        ▼
       │   ┌──────────────────────────┐
       │   │ Codex GitHub App          │
       │   │ 根据 Review 继续修改     │
       │   └──────────────────────────┘
       │
       └───────────⟲（回到步骤 5）


✅ 1.Claude「监听」Codex 的方式是什么？

    不是监听 IDE / Cloud 运行状态，而是：

    监听 GitHub 事件

	•	Issue label
	•	PR opened / synchronize
	•	PR comment / review

   👉 GitHub = 状态机 + 事件总线

✅ 2. Codex 在哪里执行？用谁的 token？
```
@codex
  ↓
Codex Cloud Runtime
  ↓
消耗 ChatGPT / Codex 配额
```
	•	❌ 不读 OPENAI_API_KEY
	•	❌ 不走 OpenAI API 计费
	•	✅ 走 Codex Cloud（产品级）

✅ 3. Claude 在哪里执行？
	•	Claude 不跑代码
	•	只做：
	•	Plan
	•	Review
	•	Merge 决策
	•	通过 Claude Code GitHub App + Actions

✅ 4. 整个系统里「唯一需要你介入的点」

你只需要：
	1.	提需求（Issue）
	2.	确认 Plan（可自动也可人工）
	3.	最终合并（可自动）

其他全部是异步自动流转。

推荐你在 Repo 里明确写下这条“协作契约”

Claude 决策，Codex 执行，GitHub 记账

放在 CONTRIBUTING.md 里，效果会非常好。